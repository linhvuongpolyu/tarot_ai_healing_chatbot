# Copy to .env and edit values
OPENAI_API_KEY=
# Example: https://openrouter.ai/api/v1 (or leave empty to use default)
OPENAI_BASE_URL=
# Optional model override; leave blank to use the default in app.py
MODEL=
